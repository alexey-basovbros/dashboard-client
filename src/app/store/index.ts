import { Store, RootStoreDispatchType, RootStoreType } from "./store";

import AuthSliceActions from "./slices/auth"

export {
  Store,
  AuthSliceActions
};

export type {
  RootStoreDispatchType,
  RootStoreType
};