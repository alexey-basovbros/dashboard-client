import { FC, ReactNode } from "react";

interface Props {};

const SettingsPage: FC<Props> = (): ReactNode => {
  return <div className="page settings">
    Settings page
  </div>
};

export {
  SettingsPage
};