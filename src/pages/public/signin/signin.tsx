import { FC, ReactNode } from "react";

interface Props {};

const SignInPage: FC<Props> = (): ReactNode => {
  return <div className="page sign-in">
    SignIn page
  </div>
};

export {
  SignInPage
};