import { FC, ReactNode } from "react";

interface Props { };

const SignUpPage: FC<Props> = (): ReactNode => {
  return <div className="page signup">
    SignUp page
  </div>
};

export {
  SignUpPage
};