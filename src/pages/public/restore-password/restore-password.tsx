import { FC, ReactNode } from "react";

interface Props {};

const RestorePasswordPage: FC<Props> = (): ReactNode => {
  return <div className="page restore-password">
    Restore password page
  </div>
};

export {
  RestorePasswordPage
};