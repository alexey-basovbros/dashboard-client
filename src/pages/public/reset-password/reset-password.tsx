import { FC, ReactNode } from "react";

interface Props {};

const ResetPasswordPage: FC<Props> = (): ReactNode => {
  return <div className="page reset-password">
    Reset password page
  </div>
};

export {
  ResetPasswordPage
};